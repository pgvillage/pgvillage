---
- name: Flush handlers
  meta: flush_handlers

- name: Start stolon systemd services
  become: yes
  systemd:
    name: "stolon-{{ item }}.service"
    state: started
    enabled: yes
    daemon_reload: yes
  loop: "{{ stolon_services }}"
