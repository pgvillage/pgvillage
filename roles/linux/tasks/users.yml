---
#- name: Create groups
#  group:
#    name: "{{ item }}"
#    system: "true"
#    state: present
#  loop: "{{ linux_users }}"

- name: Create users
  user:
    name: "{{ item }}"
#    group: "{{ item }}"
    create_home: "true"
    system: "true"
    state: present
  loop: "{{ linux_users }}"
