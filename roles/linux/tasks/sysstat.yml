---
- name: Ensure sysstat is installed
  ansible.builtin.dnf:
    name: sysstat
    state: present

- name: Ensure sysstat is running
  ansible.builtin.systemd:
    name: sysstat
    state: started
    enabled: "true"
